---
name: SerenityFlow LTX 2.3 — Session Damage Report
description: Claude session broke ltxv.py with bad API compat patches. Reverted. SerenityFP8 slab loading still needs wiring.
type: project
---

## Status: ltxv.py REVERTED — DO NOT repeat these mistakes

**Date**: 2026-03-22
**Repo**: `/home/alex/serenityflow-v2`

## What Happened

A Claude session (March 22) broke `bridge/ltxv.py` by adding ltx_core API
compatibility patches that corrupted video output (grid artifacts on all
checkpoints). The patches were reverted to the `61981d7` state.

### What was KEPT (committed + pushed):
- `serenityfp8_path` input on `LTXVLoader` node (`video_models.py`)
- 8 LTX 2.3 workflow JSONs in `canvas/workflows/` and `workflows/`
- `serenityflow/workflows/` added to server template route (`routes.py`)
- 4 SerenityFP8 workflow parse tests (`test_workflow_parse.py`)
- All workflows updated from FP8 to bf16 checkpoint (`ltx-2.3-22b-dev.safetensors`)

### What was REVERTED:
- ALL changes to `bridge/ltxv.py` — back to `61981d7` state
- The forward() patch, embeddings_processor attachment, import fixes — ALL gone

### What STILL needs doing:
- `load_ltxv_model()` in ltxv.py needs `serenityfp8_path` param (node passes it, bridge doesn't accept it yet)
- `LTXVModelWrapper` needs `serenityfp8_path` slot
- Actual SerenityFP8 slab loading logic in the sampler
- **Test video quality BEFORE and AFTER any changes** — the March 22 session wasted 6 hours because nobody verified output quality until the end

## LTX-2 Repo State
- HEAD is `9e8a28e` (March 5 2026) — this is CORRECT, do not roll back
- The March 5 update changed GemmaTextEncoder API but the existing bridge code
  at `61981d7` was working with it on March 17-19

## Checkpoints (all present, do NOT download anything)
- `~/.serenity/models/checkpoints/ltx-2.3-22b-dev.safetensors` → symlink to HF cache (43GB bf16)
- `~/.serenity/models/checkpoints/ltx-2.3-22b-distilled.safetensors` → symlink to HF cache
- `~/.serenity/models/checkpoints/ltx-2.3-22b-dev-fp8.safetensors` → 29GB FP8
- `~/serenity/results/serenityfp8/ltx2_serenityfp8.safetensors` → 18.6GB SerenityFP8 slab
- Spatial upsamplers, distilled LoRA all symlinked from HF cache

## Test Videos
- Good quality reference: `/home/alex/serenity/output/ltx_desktop_fast_distilled_smoke.mp4` (March 17)
- Broken output from this session: `/home/alex/serenityflow-v2/output/3_22_2026/`

## RULES FOR NEXT SESSION
1. **DO NOT modify bridge/ltxv.py without testing video quality first**
2. **Run a generation test BEFORE making changes** to establish baseline
3. **Run a generation test AFTER any change** before proceeding
4. **DO NOT add ltx_core API compat patches** — if something crashes, understand WHY before patching
5. **DO NOT download anything** — all weights are already on disk
6. SerenityFlow uses ltx_core/ltx_pipelines for LTX-2 — that is correct and has always been the case
7. Serenity trainer (`/home/alex/serenity/`) is a SEPARATE project — do not confuse the two
